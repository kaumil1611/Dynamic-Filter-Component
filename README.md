# Dynamic Filter Component System

A dynamic filter component system built with React.

## Setup

1.  Install dependencies:
    ```bash
    npm install
    ```

2.  Run the development server:
    ```bash
    npm run dev
    ```
